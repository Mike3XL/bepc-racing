bepc/
  __init__.py
  models.py          # dataclasses: RaceInfo, RacerResult, SeasonState
  handicap.py        # handicap engine (par racer, new handicap calc)
  points.py          # points calculation
  loader.py          # load common.json files
  processor.py       # orchestrates: load → handicap → points → season data.json
  fetcher.py         # download from WebScorer API
  generator.py       # render HTML pages from data.json
  publisher.py       # push site/ to GitHub Pages

data/
  raw/               # raw WebScorer JSON (downloaded)
  common/            # normalized per-race JSON (from prototype)

site/                # generated static site (gitignored — regenerated by CLI)
  data.json
  index.html
  standings.html
  trajectories.html
  racer/
    <name>.html

cli.py               # entry point: bepc fetch|process|generate|publish|update
requirements.txt
.gitignore
